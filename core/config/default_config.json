{
  "user_id": "00000000-0000-0000-0000-000000000001",
  "mcp": {
    "ccxt": {
      "enabled": true,
      "config_path": "core/config/ccxt-accounts.json",
      "default_exchange": "bitmex"
    },
    "indicators": {
      "enabled": true,
      "script_path": "core/mcp/servers/crypto-indicators-mcp/index.js",
      "exchange_name": "bitmex"
    }
  },
  "extraction": {
    "data_sources": [
      {
        "name": "tradingview",
        "enabled": true,
        "config": {
          "pairs": ["BTC/USD"],
          "timeframes": ["15m", "1h", "4h"],
          "indicators": ["ggshot"]
        }
      },
      {
        "name": "yfinance",
        "enabled": true,
        "config": {
          "pairs": ["BTC-USD"],
          "timeframes": ["15m", "1h", "4h"]
        }
      },
      {
        "name": "ccxt_mcp",
        "enabled": true,
        "config": {
          "exchange_id": "bitmex",
          "pairs": ["BTC/USD", "ETH/USD"],
          "timeframes": ["15m", "1h", "4h"]
        }
      }
    ],
    "indicators": [
      {
        "name": "rsi",
        "enabled": true,
        "provider": "indicators_mcp",
        "config": {
          "period": 14
        }
      },
      {
        "name": "macd",
        "enabled": true,
        "provider": "indicators_mcp",
        "config": {
          "fastPeriod": 12,
          "slowPeriod": 26,
          "signalPeriod": 9
        }
      },
      {
        "name": "bollinger_bands",
        "enabled": true,
        "provider": "indicators_mcp",
        "config": {
          "period": 20,
          "stdDev": 2.0
        }
      }
    ]
  },
  "decision": {
    "llm_provider": "deepseek",
    "strategy": "Trade momentum breakouts using ggshot as the primary signal. Be aggressive in trending markets, cautious in ranges. Look for confluence with RSI and MACD but don't be too rigid. Trust strong signals. Hold winners longer, cut losers quickly. Focus on BTC/USD with 4h as primary timeframe, 1h for entries.",
    "risk_guidelines": "Max position size 5% of capital. Max leverage 10x. Stop trading after 3 losses in a day or 5% daily drawdown.",
    "additional_context": "I prefer catching big moves over frequent small trades. The account typically holds 1-3 positions at a time."
  },
  "execution": {
    "exchange": "ccxt_mcp",
    "exchange_id": "bitmex",
    "authentication": "api_key"
  }
}