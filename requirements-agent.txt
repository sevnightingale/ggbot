# Agent Service Dependencies (Isolated from Main App)
#
# This file contains dependencies ONLY for the autonomous trading agent service.
# The agent runs in a separate virtual environment (.venv-agent) to avoid conflicts.
#
# Install: python3 -m venv .venv-agent && source .venv-agent/bin/activate && pip install -r requirements-agent.txt

# Claude Agent SDK (core dependency)
claude-agent-sdk==0.1.5

# Database access (Supabase PostgreSQL)
psycopg2-binary==2.9.10

# Redis for message queue
redis==5.2.1

# HTTP client for API calls to main ggbot service
httpx==0.28.1

# Environment variables
python-dotenv==1.1.0

# Logging
loguru==0.7.3

# Type hints
typing-extensions==4.12.2

# JSON handling
orjson==3.10.16

# Date/time utilities
python-dateutil==2.9.0.post0

# Async console for chat interface
aioconsole==0.8.2
